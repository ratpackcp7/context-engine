# Task 002 Report: Project Scaffold, Dependencies, Config, DB Schema, Pydantic Models

## STATUS: COMPLETE

## CONCERNS
- `sqlite3` CLI not available on system — acceptance tests 4 & 5 verified via Python's sqlite3 module instead.
- `staleness_days` column intentionally omitted from bullets table (spec says computed on read, not stored).
- No conflicts or ambiguities encountered.

## FILES MODIFIED
- `requirements.txt` — pinned deps from SPEC.md
- `.env.example` — all env vars including per-category staleness thresholds (Review Fix 4)
- `src/__init__.py` — empty init
- `src/harvester/__init__.py` — empty init
- `src/api/__init__.py` — empty init
- `src/config.py` — Pydantic Settings class with all env vars
- `src/models.py` — All 16 Pydantic models: ProjectCreate, ProjectResponse, ProjectListItem, ProjectListResponse, SessionCreate, SessionResponse, BulletResponse, BulletFeedback, HarvestedItem, BulletAdd, BulletUpdate, BulletArchive, CompileDelta, CompileRequest, CompileRunResponse, DigestResponse, HealthResponse
- `src/database.py` — init_db(), get_db(), 5 tables (projects, bullets, sessions, digests, compile_runs), WAL+busy_timeout+foreign_keys pragmas
- `src/templates/compile_prompt.md` — Jinja2 compile prompt placeholder
- `src/templates/digest_telegram.md` — Telegram digest template placeholder
- `src/templates/digest_email.html` — Email digest template placeholder
- `data/.gitkeep` — keep data directory in git
- `venv/` — Python 3.12 virtualenv with all deps installed

## VERIFICATION
1. ✅ `from src.config import Settings` — Config OK
2. ✅ `from src.models import CompileDelta, HarvestedItem, SessionCreate` — Models OK
3. ✅ `asyncio.run(init_db('data/test.db'))` — DB OK
4. ✅ 5 tables present: bullets, compile_runs, digests, projects, sessions
5. ✅ journal_mode = wal
