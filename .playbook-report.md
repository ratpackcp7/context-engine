# Task 007: Telegram Alerts + Email Summary Digest

## STATUS: COMPLETE

## CONCERNS
- **Gmail app password not yet configured**: `GMAIL_APP_PASSWORD` in .env is required for live email sends. Chris needs to generate one at myaccount.google.com > Security > App passwords.
- **Telegram bot shared with OpenClaw**: Using the same bot token. Digest messages go to TELEGRAM_CHAT_ID=8697133440. No conflict expected but worth noting.
- **Blocker staleness threshold = 0**: All blockers are always counted as stale per spec (Review Fix 4). This means any active blocker bullet will trigger a Telegram alert on every digest run.

## FILES MODIFIED
- `src/templates/digest_telegram.md` — Rewrote Jinja2 template: compact format with stale item list, blocker count, 4096-char aware
- `src/templates/digest_email.html` — Rewrote Jinja2 HTML template: per-project sections (status, open tasks, stale items, decisions, blockers, next steps), inline CSS for email clients
- `src/digest.py` — NEW: `generate_digest()`, `send_telegram()`, `send_email()`, `run_digest()` with full error isolation
- `tests/test_digest.py` — NEW: 15 tests covering template rendering, send success/failure, orchestration logic, DB persistence

## VERIFICATION
1. `python -c "from src.digest import generate_digest, run_digest; print('Digest OK')"` — PASS
2. `pytest tests/test_digest.py -v` — 15/15 PASSED
3. Template renders correctly with stale items, no stale items, and multiple projects (3 template tests)
4. Telegram failure does not prevent email send (dedicated test)
5. Digest record saved to DB even when both sends fail (dedicated test)
