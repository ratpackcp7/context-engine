# Task 005 Report: ACE-inspired compile loop with delta updates

## STATUS: COMPLETE

## CONCERNS
- The `llm_provider` and `llm_model` fields in compile_runs are not populated yet — LLMClient.compile() returns a CompileDelta but doesn't expose which provider succeeded. Can be addressed when the compile API route is built.
- The compile loop creates a new DB connection per run via `get_db()`. When integrated into FastAPI, consider using a shared connection pool or the app's lifespan-managed connection.

## FILES MODIFIED
- `src/templates/compile_prompt.md` — Enhanced Jinja2 template with project_name var, explicit JSON-only output rules, category/source value constraints, and empty-delta instruction
- `src/compiler.py` — NEW: Full compile loop with asyncio.Lock, harvest→LLM compile→apply deltas→staleness scan, local session harvesting, compile_runs recording
- `tests/test_compiler.py` — NEW: 12 tests covering delta application (add/update/archive), invalid bullet_id handling, staleness scan (task/blocker/decision thresholds), full run_compile integration with mocked LLM/harvesters
- `tests/conftest.py` — Created (shared test fixtures placeholder)

## VERIFICATION
1. `python -c "from src.compiler import run_compile; print('Compiler OK')"` — passes
2. `pytest tests/test_compiler.py -v` — 12/12 passed
3. Mock LLM add/update/archive → DB reflects changes correctly (test_compile_with_valid_delta)
4. Bullet older than staleness threshold → status set to "stale" (test_stale_task, test_blocker_always_stale, test_decision_staleness)
5. Invalid bullet_id in update array → skipped with warning, no crash (test_invalid_bullet_id_skipped)
6. Empty harvest data → LLM not called (test_empty_harvest_skips_llm)
7. LLM failure → compile skips gracefully (test_llm_failure_graceful)
8. Compile run recorded in compile_runs table (test_compile_records_run)
