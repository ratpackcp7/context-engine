# Task 006 Report: FastAPI app, endpoints, auth middleware

## STATUS: COMPLETE

## CONCERNS
- `src/dependencies.py` was created (not in SPEC file tree) to resolve circular imports between `main.py` and API routers. Standard FastAPI pattern.
- POST /compile returns `compile_run_id: "pending"` since the run_id is generated inside the background task. Callers should poll compile_runs table.
- Test fixture uses deprecated `asyncio.get_event_loop()` — works but emits a DeprecationWarning.

## FILES MODIFIED
- `src/main.py` — NEW: FastAPI app with lifespan handler, health endpoint, router includes
- `src/dependencies.py` — NEW: Shared auth (bearer token) and settings dependencies
- `src/api/sessions.py` — NEW: POST /sessions endpoint
- `src/api/projects.py` — NEW: GET /projects, POST /projects, GET /projects/{slug}/playbook, POST /bullets/{id}/feedback
- `src/api/compile.py` — NEW: POST /compile (BackgroundTask), GET /compile/{run_id}
- `src/api/digest.py` — NEW: GET /digest/latest
- `tests/test_api.py` — NEW: 15 tests covering all endpoints and auth

## VERIFICATION
1. `pytest tests/test_api.py -v` — 15/15 passed
2. `uvicorn src.main:app --port 8410` — starts without errors
3. GET /health returns 200 (no auth required)
4. GET /projects returns 401 without token, 200 with valid token
5. POST /projects creates project, GET /projects lists it
6. POST /sessions with valid slug returns 201, invalid slug returns 404
7. GET /projects/{slug}/playbook returns markdown with all 7 required sections (Current Status, Open Tasks, Recent Decisions, Active Blockers, Technical State, Stale Items, Next Steps)
8. POST /compile returns 202
9. POST /bullets/{id}/feedback returns 404 for missing bullet
