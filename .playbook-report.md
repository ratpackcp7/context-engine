# Task 004 Report: Notion + LCM-Lite Data Harvesters

## STATUS: COMPLETE

## CONCERNS
- Notion property names (Status, Project, Tags, Notes, Summary) are best-guess based on common Notion setups. Actual DB schemas may differ — extractors handle missing properties gracefully.
- LCM-Lite response format assumed from task notes (`/search?q=...`). Response field names handled flexibly with fallbacks.
- Notion API version header pinned to `2022-06-28` as specified. Newer data source API (2025-09-03) exists but was not required.
- Rate limit delay (400ms) uses `asyncio.sleep` — correct in async context but won't throttle concurrent coroutines.

## FILES MODIFIED
- `src/harvester/__init__.py` — `harvest_all()` with deduplication by `source:source_id`
- `src/harvester/notion.py` — NEW: `harvest_todos()`, `harvest_sessions()` with pagination, rate limiting, project filtering
- `src/harvester/lcm.py` — NEW: `harvest_lcm()` with since filtering and error handling
- `tests/test_harvester.py` — NEW: 10 tests covering all acceptance criteria

## VERIFICATION
1. `python -c "from src.harvester import harvest_all; print('Harvester OK')"` — passes
2. `pytest tests/test_harvester.py -v` — 10/10 passed
3. Notion pagination test: 2-page mock with start_cursor returns combined results
4. Deduplication test: same source+source_id deduped, different sources preserved
5. Since filter test: verifies Notion API timestamp filter body structure
6. Project filter test: items tagged for different project excluded
7. LCM error handling test: HTTP 500 returns empty list (no crash)
